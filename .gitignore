# Environment variables, which can include sensitive information (DISCOURSE_API_KEY).
.env

# Gem packages as installed during the last run of "bundle install".
/Gemfile.lock

# NPM package list and packages, as installed during the last run of "npm install".
/package-lock.json
node_modules

# IDE configuration files.
/.idea
/.vscode
*.code-workspace

# Apple macOS Desktop Services Store (icon positions etc. of parent folder).
.DS_Store

# Output directory of the finished website.
/build

/.tmp

*.log
